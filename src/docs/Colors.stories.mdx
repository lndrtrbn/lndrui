import { Canvas, Meta, Story } from "@storybook/addon-docs"

import Colors from "./components/Colors/Colors"
import { colors } from "../styles/design-system/colors"

<Meta title="Design System/Colors" />

# Colors

The Design System uses several color palettes listed bellow.

### Jungle Green

<Colors values={colors.jg} />

### Brick Red

<Colors values={colors.br} />

### Neutral

<Colors values={colors.neutral} />

### Midnight Blue

<Colors values={colors.mb} />

The main color is `Jungle Green`, `Brick Red` is for accent and dangerous actions.

The `Neutral` color is for backgrounds and fonts in light theme while `Midnight Blue` is for dark theme.
